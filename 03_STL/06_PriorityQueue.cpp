#include<bits/stdc++.h>
using namespace std;

int main() {
  // Priority_Queue -> Sorted Queueu.
  // Syntax->
  // Declaration->
  // stack<datatype>'name';
  
  // Methods->
  // 1. .push(k)-> Pushes element k to the top of the stack.
  // 2. .pop()-> Pops out the element from the top of the stack.
  // 3. .top()-> Returns the top element from stack.
  // 3. .empty()-> Returns true if empty else false.
  return 0;
}