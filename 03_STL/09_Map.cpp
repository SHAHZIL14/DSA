int main() {
  // MAP -> Stores {Key, Value} pairs. Keys are unique and sorted.

  // Syntax ->
  // Declaration -> map<key_datatype, value_datatype> name;

  // !!Methods ->
  // 1. name[key] = value -> Inserts or updates the value for that key.
  // 2. insert({key, value}) -> Standard pair insertion.
  
  // 3. Accessing ->
  //    - name[key] -> Returns the value associated with the key.
  //    - For an iterator 'it', it->first is the key, it->second is the value.

  // 4. find(key) -> Returns iterator to the pair.
  
  return 0;
}