#include<bits/stdc++.h>
using namespace std;

int main() {
  // LIST -> Same as vector but provides front operations also.
  // Syntax -> list <datatype> 'name';
  // Extra methods->
  // 1. push_front(k)-> Adds k at the beginning of the list. 
  // 1. emplace_front(k)-> Adds k at the beginning of the list but faster. 
  return 0;
}