#include<bits/stdc++.h>
using namespace std;

int main() {
  // QUEUE -> Follows FIFO first in first out.
  // Syntax->
  // Declaration->
  // queue<datatype>'name';
  
  // Methods->
  // 1. .push(k)-> Pushes element k to the last of the queue.
  // 2. .pop()-> Pops out the element from the front of the queue.
  // 3. .front()-> Returns the front element from queue.
  // 3. .back()-> Returns the element from the back of the queue.
  return 0;
}