int main() {
  // MULTISET -> Stores non-unique elements in sorted order.

  // Syntax ->
  // Declaration -> multiset<datatype> name;

  // !!Methods ->
  // 1. insert(k) -> Adds k. Multiple instances of k are allowed.
  
  // 2. Erasing ->
  //    - erase(value) -> !!WARNING!! Deletes ALL instances of that value.
  //    - erase(name.find(value)) -> Deletes only a SINGLE instance of that value.

  // 3. count(k) -> Returns the actual frequency of k in the multiset.
  
  return 0;
}