#include<iostream>
using namespace std;

int main() {
  // UNORDERED MAP -> Keys are unique but NOT sorted.

  // Syntax ->
  // Declaration -> unordered_map<key_type, value_type> name;

  // !!Key Differences ->
  // 1. Time Complexity -> Average O(1), Worst Case O(n) (rare collisions).
  // 2. Map vs Unordered -> Use Map if order matters; use Unordered for speed.
  // 3. Note -> Does not support lower_bound or upper_bound.

  return 0;
}